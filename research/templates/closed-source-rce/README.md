# Closed-source RCE workspace templates

Copy `DISCOVERY-PROMPT.md` to `research/active/closed-source-rce/DISCOVERY-PROMPT.md`, then copy `target-instance/` to a target-specific local directory. Replace every bracketed value before analysis.

The copied files are local case state and must not be committed. Keep exact tested identity and raw artifacts in the target instance, while the repository root remains target-independent.
