# Evidence directory

Store raw CDB logs, dumps, PoC/control files, exact commands, hashes, screenshots, and reproducibility outputs here. Keep raw artifacts immutable when practical. Summaries and finding cards must point here rather than copy or rewrite the evidence.
